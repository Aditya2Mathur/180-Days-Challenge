import React from "react";
import "./College.css";

const College = (props) =>{
    let branch = props(branch);
}
return (
    <div className="college">
        <div>{branch}</div>
        <div>{year}</div>
    </div>
)
export default College;